#include <stdio.h>

/**
 * add - function that adds sum of int
 * @sum1: input first sum to be added
 * @sum2: input second sum to be added
 * Return: sum1 + sum2
 */

int add(int sum1, int sum2)
{
	return (sum1 + sum2);
}
